defmodule AinComBooking.Repo.Migrations.SupportFlexibleBookingTargets do
  @moduledoc false
  use Ecto.Migration

  # The schema is created in its final form by the earlier create migrations.
  # This historical migration remains as a no-op so existing migration order stays stable.
  def up, do: :ok
  def down, do: :ok
end
